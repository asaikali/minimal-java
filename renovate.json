{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Renovate config for minimal-java. Extends the recommended preset; see https://docs.renovatebot.com/configuration-options/ for tuning options (auto-merge policies, schedules, grouping rules). See docs/renovate.md for how this repo's setup fits together.",
  "extends": [
    "config:recommended"
  ],
  "prHourlyLimit": 0,
  "prConcurrentLimit": 10,
  "packageRules": [
    {
      "description": "Major bumps land on the Dependency Dashboard as a checkbox instead of opening a PR automatically. Tick the box, run Renovate, and it opens the PR — keeps the review queue free of breaking-change PRs you didn't ask for.",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Base images use ${ARG}-composed tags (eclipse-temurin:${JAVA_VERSION}-jre in images/{jre,fat,app,jvm-aot,spring-aot}/Dockerfile, ubuntu:${UBUNTU_VERSION} in images/ubuntu/Dockerfile). Renovate's Docker manager can DETECT these but cannot rewrite the literal tag in place, so non-major PRs just fail to create and clutter the dashboard. Disable non-major updates for them — the Dockerfiles keep their clean major tags (25, 26.04), and a new MAJOR still surfaces on the dashboard so you can bump the ARG by hand. (chisel is unaffected: it's the custom 'regex' manager, which edits its ARG line directly.)",
      "matchManagers": ["dockerfile"],
      "matchPackageNames": ["eclipse-temurin", "ubuntu"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest", "pinDigest"],
      "enabled": false
    }
  ],
  "customManagers": [
    {
      "description": "Track the chisel release pinned in images/ubuntu/Dockerfile's CHISEL_VERSION ARG. chisel is fetched from a GitHub release URL (not referenced as an image tag), so the Docker manager can't see it — the `# renovate:` annotation above the ARG plus this regex manager edit that ARG line directly. The managerFilePatterns regex matches any path ending in Dockerfile, so images/*/Dockerfile is covered. Unlike the ubuntu/temurin base images (whose ${ARG}-composed tags the Docker manager can detect but not rewrite), chisel updates apply cleanly because the version IS the whole ARG value.",
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)Dockerfile$/"],
      "matchStrings": [
        "#[ \\t]*renovate:[ \\t]*datasource=(?<datasource>\\S+)[ \\t]+depName=(?<depName>\\S+)(?:[ \\t]+versioning=(?<versioning>\\S+))?[ \\t]*\\r?\\nARG[ \\t]+\\w+=(?<currentValue>\\S+)"
      ],
      "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver{{/if}}"
    }
  ]
}
